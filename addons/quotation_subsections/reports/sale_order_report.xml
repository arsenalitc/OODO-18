<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit the main quotation report template -->
    <template id="report_saleorder_document_subsection" inherit_id="sale.report_saleorder_document">
        <!-- Replace the order lines section to handle subsections -->
        <xpath expr="//tbody[@class='sale_tbody']" position="replace">
            <tbody class="sale_tbody">
                <t t-set="current_section" t-value="None"/>
                <t t-set="current_subsection" t-value="None"/>
                <t t-set="lines" t-value="doc.order_line.sorted(key=lambda l: l.sequence)"/>
                
                <t t-foreach="lines" t-as="line">
                    <!-- Section Header -->
                    <t t-if="line.display_type == 'line_section'">
                        <t t-set="current_section" t-value="line"/>
                        <t t-set="current_subsection" t-value="None"/>
                        <tr class="is-subtotal text-right">
                            <td colspan="99" class="text-right">
                                <strong t-field="line.name" class="text-left"/>
                            </td>
                        </tr>
                        <t t-if="not line_last and lines[line_index+1].display_type == 'line_note'">
                            <tr>
                                <td colspan="99">
                                    <span t-field="lines[line_index+1].name"/>
                                </td>
                            </tr>
                        </t>
                    </t>
                    
                    <!-- Subsection Header -->
                    <t t-elif="line.display_type == 'line_subsection'">
                        <t t-set="current_subsection" t-value="line"/>
                        <tr class="is-subtotal text-right" style="background-color: #f8f9fa;">
                            <td colspan="3" class="text-left" style="padding-left: 20px;">
                                <strong t-field="line.name"/>
                            </td>
                            <td class="text-right">
                                <strong>Subtotal: </strong>
                            </td>
                            <td class="text-right">
                                <strong t-field="line.subsection_total" 
                                        t-options='{"widget": "monetary", "display_currency": doc.pricelist_id.currency_id}'/>
                            </td>
                        </tr>
                        <t t-if="not line_last and lines[line_index+1].display_type == 'line_note'">
                            <tr>
                                <td colspan="99" style="padding-left: 20px;">
                                    <span t-field="lines[line_index+1].name"/>
                                </td>
                            </tr>
                        </t>
                    </t>
                    
                    <!-- Note lines -->
                    <t t-elif="line.display_type == 'line_note'">
                        <tr>
                            <td colspan="99">
                                <span t-field="line.name"/>
                            </td>
                        </tr>
                    </t>
                    
                    <!-- Regular product lines -->
                    <t t-else="">
                        <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-weight-bold o_line_note' if line.display_type == 'line_note' else ''">
                            <t t-if="not line.display_type">
                                <td name="td_name">
                                    <span t-field="line.name"/>
                                </td>
                                <td name="td_quantity" class="text-right">
                                    <span t-field="line.product_uom_qty"/>
                                    <span t-field="line.product_uom" groups="uom.group_uom"/>
                                </td>
                                <td name="td_priceunit" class="text-right">
                                    <span t-field="line.price_unit"/>
                                </td>
                                <td name="td_discount" t-if="display_discount" class="text-right">
                                    <span t-field="line.discount"/>%
                                </td>
                                <td name="td_taxes" class="text-right">
                                    <span t-esc="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                                </td>
                                <td name="td_subtotal" class="text-right o_price_total">
                                    <span t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                                    <span t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                                </td>
                            </t>
                        </tr>
                    </t>
                </t>
            </tbody>
        </xpath>
    </template>

    <!-- Enhanced quotation report with subsection support -->
    <template id="report_saleorder_subsection" inherit_id="sale.report_saleorder">
        <xpath expr="//t[@t-call='sale.report_saleorder_document']" position="attributes">
            <attribute name="t-call">quotation_subsections.report_saleorder_document_subsection</attribute>
        </xpath>
    </template>

</odoo>
